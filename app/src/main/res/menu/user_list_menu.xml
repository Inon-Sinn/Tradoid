<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/menu_user_search"
        app:showAsAction="ifRoom|collapseActionView"
        android:icon="@drawable/ic_white_search"
        app:iconTint="@color/white"
        android:title="@string/search"
        app:actionViewClass="android.widget.SearchView" />


    <item
        app:showAsAction="ifRoom|collapseActionView"
        android:icon="@drawable/ic_white_menu"
        app:iconTint="@color/white"
        android:title="">

        <menu>

            <item android:id="@+id/menu_stock_market"
                android:title="@string/stock_market"
                android:icon="@drawable/ic_trending_up"
                app:showAsAction="never"/>

            <item android:id="@+id/menu_status_pg"
                android:title="@string/status_page"
                android:icon="@drawable/ic_wallet"
                app:showAsAction="never"/>

            <item android:id="@+id/menu_user_settings"
                android:title="@string/settings"
                android:icon="@drawable/ic_settings"
                app:showAsAction="never"/>

            <item android:id="@+id/menu_user_profile"
                android:title="@string/profile"
                android:icon="@drawable/ic_identity"
                app:showAsAction="never"/>

            <item android:id="@+id/menu_user_logout"
                android:title="@string/logout"
                android:icon="@drawable/ic_logout"
                app:showAsAction="never"/>

        </menu>
    </item>
</menu>